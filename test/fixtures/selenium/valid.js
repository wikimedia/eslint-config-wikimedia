'use strict';

const el = $( '.el' );
const menu = $$( '#menu' );

browser.execute( () => {
	// Client code
} );

// Off: no-mocha-arrows
describe.skip( () => {
	// Off: mocha/no-hooks-for-single-case
	beforeEach( () => {
	} );
	// Off: mocha/no-skipped-tests
	it.skip( () => {
	} );
} );
