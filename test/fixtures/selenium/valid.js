'use strict';

const el = $( '.el' );
const menu = $$( '#menu' );

browser.execute( () => {
	// Client code
} );

// Rule: no-mocha-arrows
describe.skip( () => {
	// Rule: mocha/no-hooks-for-single-case
	beforeEach( () => {
	} );
	// Rule: mocha/no-skipped-tests
	it.skip( () => {
	} );
} );
