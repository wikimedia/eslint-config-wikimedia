'use strict';

// Recommended
// eslint-disable-next-line mocha/no-global-tests
it( 'global', () => {} );

describe( 'foo', () => {} );

// eslint-disable-next-line mocha/max-top-level-suites
describe( 'bar', () => {} );
